import React from "react";
import Parent from "./Parent";

//! props (objects) --> properties (used to send data from one component to another component.)
const Props = () => {
  return (
    <div>
      <Parent />
    </div>
  );
};

export default Props;
