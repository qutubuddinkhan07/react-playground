import React from "react";
import ReactDOM from "react-dom/client";
// import { App } from "./app"; // named import
import App from "./App";

// ReactDOM.createRoot(document.getElementById("root")).render(App);
// function names cannot be passed here

ReactDOM.createRoot(document.getElementById("root")).render(<App />);
